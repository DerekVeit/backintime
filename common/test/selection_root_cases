
:none  original-fails
    includes
        /
        /etc/issue
    excludes
        /**
        *.pyc
    files_tree
    expected_tree
        etc/
            issue

:none2  sorted-only
    includes
        /
        /etc/issue
    excludes
        /           # This has no effect in the original selection mode, so /** would be included.
        *.pyc
    files_tree
    expected_tree
        etc/
            issue
